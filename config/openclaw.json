{
  "models": {
    "providers": {
      "myprovider": {
        "baseUrl": "${MYPROVIDER_BASE_URL}",
        "apiKey": "${MYPROVIDER_API_KEY}",
        "api": "openai-completions",
        "models": [
          { "id": "claude-opus-4-6", "name": "claude-opus-4-6" }
        ]
      }
    }
  },
  "agents": {
    "defaults": {
       "model": {
         "primary": "openai-codex/gpt-5.3-codex"
       },
      "sandbox": {
        "mode": "off"
      },
      "subagents": {
        "maxSpawnDepth": 2,
        "maxConcurrent": 3,
        "maxChildrenPerAgent": 3
      }
    }
  },
  "tools": {
    "web": {
      "search": { "enabled": true },
      "fetch": { "enabled": true }
    },
    "allow": ["group:ui", "group:runtime", "group:web"]
  },
  "channels": {
    "telegram": {
      "enabled": true,
      "dmPolicy": "pairing",
      "groups": {
        "*": {
          "groupPolicy": "open",
          "requireMention": false
        }
      }
    },
    "zalo": {
      "enabled": false,
      "botToken": "${ZALO_BOT_TOKEN}",
      "dmPolicy": "pairing"
    },
    "slack": {
      "enabled": false,
      "appToken": "${SLACK_APP_TOKEN}",
      "botToken": "${SLACK_BOT_TOKEN}",
      "mode": "socket"
    }
  },
  "gateway": {
     "port": 3000,
     "mode": "local"
  },
  "plugins": {
    "entries": {
      "telegram": {
        "enabled": true
      },
      "zalo": {
        "enabled": false
      }
    }
  }
}
